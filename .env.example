# Anthropic API key used to generate blog drafts.
# Get one at https://console.anthropic.com/
ANTHROPIC_API_KEY=

# Port the server listens on when running `npm run start` (defaults to 3000).
PORT=3000
